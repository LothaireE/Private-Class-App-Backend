services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: private_class_app
      POSTGRES_USER: private_class_app
      POSTGRES_PASSWORD: private_class_app
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
